
<head>
  <link rel="stylesheet" href="/styles/style.css">
</head>

<h1>KōtarEATS</h1>

<div class="recipe_display">
  {{#each recipes}}

  <div class="recipe_box">
    <p><a href="/recipe/{{id}}">{{title}}</a> Rated:({{rating}}*)</p>
    <img class="recipeimg" src="{{recipe_picture}}" alt="tasty food">
  </div>

  {{/each}}

  <div class="column" id="users_column">
    <ul>
      {{#each profiles}}
      <li><a href="/profile/{{profile_id}}">{{name}}</a></li>
      {{/each}}
    </ul>
    <a href="/addprofile/">Add Profile</a>
  </div>

</div>